'use strict'

export const BASE_URL = window.BASE_URL || 'http://localhost:3000'
export const PAGES_URL = `${BASE_URL}/pages-api`
export const STORAGE_PUBLIC_URL = window.STORAGE_PUBLIC_URL || '/files'
export const PREVIEW_SITE_URL = window.PREVIEW_SITE_URL || 'http://localhost:3500'
export const PREVIEW_PRD_URL = window.PREVIEW_PRD_URL || 'http://winemakers-website.prd.prw.paas.impero.me'

export const QUERY_LANG = window.QUERY_LANG || 'en'
